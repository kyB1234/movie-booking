const express = require('express');const router = express.Router();const staticController = require('../controllers/staticController');const userController = require('../controllers/userController');router.get('/', staticController.checkAuth, staticController.setUser, userController.home);router.get('/orders', staticController.checkAuth, staticController.setUser, userController.history);router.get('/active', staticController.checkAuth, staticController.setUser, userController.active);router.post('/userdata', staticController.checkAuth, staticController.setUser, userController.userdata);router.post('/update/:id', staticController.checkAuth, staticController.setUser, userController.updateUser);module.exports = router;